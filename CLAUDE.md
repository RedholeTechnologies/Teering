# Teering

코드 구조를 **시간축과 함께** 보는 로컬 전용 코드 지도. 기획은 [treering-plan.md](treering-plan.md).

---

## 브랜치 · 머지

```
feat/*  ──Squash──▶  develop  ──Merge commit──▶  main
```

1. **항상 PR.** `main` · `develop` 에 직접 push 하지 않는다. 최초 리포 생성만 예외였다.
2. **타깃이 `develop` 이면 Squash, `main` 이면 Merge commit.**
3. **커밋 메시지는 영어, 접두어로 시작한다.** `[FEAT]` / `[FIX]` / `[REFACTOR]` / `[PERF]` /
   `[TEST]` / `[CHORE]` / `[DOCS]`. `[PERF]` 는 동작은 그대로 두고 빠르게만 한 것, `[TEST]` 는
   테스트만 바꾼 것이다(소스를 고치는 테스트는 `[FIX]`). **`main` 으로 가는 릴리스 머지 커밋은
   `[RELEASE]`** — 기능 커밋은 이미 `develop` 에 제 접두로 들어가 있으니, 릴리스까지 `[FEAT]` 로
   달면 같은 변경을 두 번 세고 `main` 에서 릴리스가 보이지 않는다.
   무엇을 했는지가 아니라 **무엇이 달라졌는지**를 말한다. 아래 「커밋 메시지」 를 본다.
4. `feat/*` · `fix/*` 는 항상 `develop` 에서 딴다. hotfix 는 `main` 에서 따고
   **`develop` 에도 되돌린다.**
5. **`git pull` 은 머지 커밋을 남기지 않는다.** 전역 `pull.ff only` 로 고정 — fast-forward
   만 허용하고 갈라지면 **실패**한다. 실패하면 `git pull --rebase` 로 결정한다.
6. **리포를 처음 만들 때 `main` 에는 `LICENSE` 와 `.gitignore` 만 올린다.** 기획서·README·
   이 파일·코드는 전부 `develop` 에서 시작한다. `main` 은 릴리스된 것만 담는다.
7. **자격증명은 절대 커밋하지 않는다.** `.gitignore` 는 예방일 뿐이다 — 한 번 들어간 키는
   파일을 지우거나 무시 목록에 넣어도 **이력에 영구히 남는다.** 들어갔다면 **회전(rotate)이
   먼저**고, 이력 정리는 그다음이다. 찾을 때도 값은 출력하지 않는다(경로·커밋·키 이름까지만).
8. **`main` 으로 나간 릴리스마다 태그를 붙인다** — 릴리스 PR 을 머지한 **그 작업 안에서**.
   주석 태그 `vX.Y.Z`, 메시지는 `vX.Y.Z — <릴리스 PR 제목>`. 버전은 묻지 않고 들어간 커밋의
   접두로 정한다:

   | 들어간 것 | 올리는 자리 |
   |---|---|
   | `[FEAT]` 이 하나라도 있다, 또는 스키마·API 가 **깨지는** 변경 | **minor** (0.4.3 → 0.5.0) |
   | `[FIX]` · `[REFACTOR]` · `[PERF]` · `[TEST]` · `[CHORE]` · `[DOCS]` 뿐 | **patch** (0.4.3 → 0.4.4) |

   마이그레이션이 들어갔다는 것만으로는 minor 가 아니다. 소비자가 같이 바뀌어야 할 때만
   «깨지는» 변경이다. 1.0.0 은 운영이 안정됐다고 합의했을 때만 올린다. 소급할 때는 태그 날짜를
   머지한 날로 맞춘다(`GIT_COMMITTER_DATE="$(git log -1 --format=%cI <커밋>)" git tag -a …`).
   이미 push 한 태그는 옮기지 않는다 — 잘못 붙였으면 다음 번호로 바로잡는다.
   (이 리포는 2026-09-27 에 새로 만들었고, 번호도 `v0.1.0` 부터 다시 시작했다.)

### 커밋 메시지

- **예외가 없다.** 리포의 첫 커밋, 손으로 만든 커밋(`git commit-tree`), 빈 커밋도 같은 규칙을
  따른다. `Initial commit` 은 쓰지 않는다 — 첫 커밋은 비워 두지 않고 라이선스와 `.gitignore`
  를 담아(6번), `[CHORE] Add <라이선스> license and .gitignore` 처럼 그걸 말하는 메시지를 단다.
- **같은 문장을 두 번 남기지 않는다.** `main` 으로 가는 머지 커밋 흐름에서는 브랜치 커밋과
  머지 커밋이 둘 다 이력에 남는다. PR 제목은 변경 전체를, 브랜치 커밋은 제 몫을 말하게 해서
  같은 줄이 겹치지 않게 한다.
- **AI 서명을 넣지 않는다.** 커밋에 `Co-Authored-By`, PR 에 「Generated with …」 같은
  공동작성자·생성 표기를 붙이지 않는다. 도구가 기본으로 붙이라고 해도 그렇다.
- **올리기 전에 GitHub 에 보일 그대로 읽는다.** `git log --format='%h %s'` 로 목록을 띄워
  한 줄씩 위 규칙과 맞춰 본다. 2026-09-26 에 빈 `Initial commit` 과 PR 제목을 되풀이한
  커밋이 올라가 리포를 다시 만든 적이 있다.

### 2번이 왜 중요한가

스쿼시는 **원 작성자를 보존한다.** 남이 머지해도 커밋 작성자는 원래 사람이다.
머지 커밋은 **머지한 사람이 작성자**가 된다.

Vercel 은 커밋 작성자의 GitHub 계정에 Vercel 계정이 연결돼 있는지 보고, 없으면
`Deployment was blocked` 로 막는다. `Redeploy` 를 눌러도 같은 커밋이라 또 막힌다.
`main` 을 머지 커밋으로만 갱신하면 이 문제가 구조적으로 사라진다.
이 리포는 아직 Vercel 을 쓰지 않지만, 규칙은 조직 전체에서 같은 모양으로 유지한다.

### 강제 — 걸려 있다

이 리포는 `RedholeTechnologies` org(**Team 플랜**) 소유의 **public** 리포다.
규칙 1·2 는 문서가 아니라 **ruleset 이 기계적으로 강제한다.**

| | `main` | `develop` |
|---|---|---|
| PR 필수 | ✅ | ✅ |
| 허용 머지 방식 | **merge commit 만** | **squash 만** |
| 필요 승인 수 | 0 | 0 |
| force push | 차단 | 차단 |
| 브랜치 삭제 | 차단 | 차단 |
| 예외(bypass) | 없음 | 없음 |

직접 push 하면 `Changes must be made through a pull request.` 로 거부된다. 실측 확인했다.

**승인 수가 0 인 것은 의도한 것이다.** 1 이상이면 혼자서는 자기 PR 을 못 머지한다.
「PR 을 거쳐라」는 강제하되 「남이 봐줘야 한다」는 강제하지 않는다. 사람이 늘면 올린다.

**bypass 는 비워 뒀다.** 조직 관리자도 예외가 아니다. 급할 때 우회가 필요해지면
그때 repository admin 을 bypass 에 넣는다 — 미리 열어 두지 않는다.

---

## 공개 리포 — 다른 저장소의 흔적을 들이지 않는다

이 리포는 **public** 이다. 개발하며 Treering 을 돌려 보는 저장소들은 대부분 **비공개** 다.
그 이름·식별자·구조가 여기 들어오면 그대로 세상에 공개된다.

2026-09-26 에 한 번 샜다. 테스트 픽스처·주석·README·커밋 메시지·PR 설명에 다른 저장소의
이름과 타입·모듈 이름이 쌓여 있었다. 이름을 가명으로 바꾸고 리포를 새로 만들었는데, 가명이
실제 이름의 일부와 업무 영역을 그대로 비추고 있었다(실제 이름의 단어와 그 동의어를 가명으로
썼다). 2026-09-27 에 그것까지 걷어 내고 리포를 한 번 더 새로 만들었다. 다시 그럴 일이 없게
아래를 지킨다.

### 들이지 않는 것

파일 · 커밋 메시지 · PR 제목과 설명 · 이슈 · 코드 주석 **어디에도**:

- 다른 저장소의 이름, 로컬 경로, 조직·폴더 이름, 어셈블리·패키지 이름
- 그 코드의 타입 · 메서드 · 모듈 이름, 폴더 구조, 쓰는 라이브러리 목록
- 이름과 함께 적은 규모 수치 (파일 수 · 심볼 수 · 색인 시간)
- 그 저장소에서 뽑은 색인과 DB — `*.scip` · `*.db` 는 `.gitignore` 에 있다. 빼지 않는다.

### 대신 이렇게

- **테스트 픽스처는 지어낸 이름으로.** `Acme`, `Shop`, `Order`, `src.app` 같은 것. 실제 색인에서
  모양을 배웠어도, 옮길 때는 이름을 바꾼다.
- **가명도 실제를 비추지 않게.** 실제 이름의 단어, 그 동의어나 번역, 그 저장소의 업무 영역을
  가명에 쓰지 않는다. 가명은 이 리포의 픽스처 어휘(`Shop`·`Order`·`Invoice`)에서 고른다.
- **실측은 이름 대신 모양으로.** 「C# 저장소 (프로젝트 4개, `.cs` 632개)」.
- **커밋과 PR 전에 훑는다.** `git diff --cached` 와 PR 본문에서, 이번 세션에 가져오거나 색인한
  저장소의 이름과 그 안의 식별자를 검색한다. 하나라도 나오면 올리지 않고 바꾼다.
- 이미 올라갔으면 **덮지 말고 알린다.** 무엇이 어디에 나갔는지 사람이 보고 정한다.
