# Treering — 기획서

> 코드 구조를 **시간축과 함께** 보는 로컬 전용 코드 지도.
> 작업명 `treering`(나이테). 이름은 아직 확정 아님 — 마지막 절 참고.

---

## 한 줄

정밀한 코드 그래프를 만들고, **커밋마다 저장해서 «지난주 대비 무엇이 어떻게 엮였는지» 를 보여준다.** 전부 로컬, LLM 없음.

---

## 왜 만드나 — 기존 도구를 직접 써 보고 남은 빈자리

두 도구를 실제로 설치해 우리 C# 모노레포(프로젝트 22개 · 소스 1,169개)에 돌려 본 결과다.

| | Sourcetrail | graphify | 우리 |
|---|---|---|---|
| 정확도 | 정밀 (Roslyn) | 추정 (문법만) | **정밀** |
| 전체 지도 | 없음 | 있음 | **있음** |
| 파고들기 | 좋음 | 얕음 | **좋음** |
| 에이전트 질의 | 불가 (GUI 전용) | 가능 (MCP) | **가능** |
| 갱신 | 전량 (우리 리포 22분) | 전량 | **증분** |
| 시간축 | 없음 | 없음 | **있음** |
| 외부 전송 | 없음 | 문서는 LLM 으로 | **없음** |
| 유지 상태 | 2021년 이후 중단 | 활발 | — |

**핵심 관찰: 둘 다 «지금 이 순간» 만 본다.** 「이 클래스는 3주 전엔 아무도 안 불렀는데 지금 12곳에서 부른다」 를 보여주는 도구가 없다. 거기가 이 프로젝트의 자리다.

### 인접 도구들도 비어 있다

- **Sourcegraph** — 정밀하고 시간축도 일부 있지만 무겁고 서버·클라우드 전제.
- **NDepend** — C# 전용에 추세 그래프도 있다. 다만 상용(유료)·윈도우 전용이고 에이전트가 질의할 수 없다.
- **CodeScene** — 이력 분석은 강하지만 SaaS 이고 구조 그래프가 아니라 지표 중심.

즉 «로컬 · 무료 · 정밀 · 시간축 · 에이전트 질의» 를 한 번에 주는 게 없다.

---

## 핵심 설계 결정: 색인기를 직접 만들지 않는다

**Sourcetrail 이 죽은 이유가 언어별 색인기 유지보수다.** 같은 함정을 피해야 한다.

대신 **SCIP**(Sourcegraph Code Intelligence Protocol)를 입력으로 받는다.

- SCIP = 정밀 코드 색인 표준. Apache-2.0, 현재도 활발히 유지됨.
- 이미 존재하는 색인기:
  - `sourcegraph/scip-dotnet` — **Roslyn 기반** C# 색인기
  - `sourcegraph/scip-typescript` — TS/JS
  - 그 밖에 Java·Go·Python·Rust 등
- 우리는 SCIP 파일을 읽어 그래프로 바꾸고, 그 위의 경험을 만든다.

**결과:** 정확도는 컴파일러 급인데 언어 지원 부담은 남이 진다. 언어를 늘리는 일 = 색인기 하나 더 붙이기.

---

## 만드는 것 (이 프로젝트의 전부)

### 0. 부분 그래프 — 나머지 셋이 전부 이 위에 올라간다

**전체 그래프를 메모리에 올리는 코드는 한 줄도 쓰지 않는다.** UI·CLI·MCP 가 쓰는 원시연산은 하나뿐이다.

```
subgraph(seeds, direction, depth, kinds, granularity, budget)
    -> { nodes, edges, truncated }
```

- `seeds` — 시작점 몇 개 (심볼 · 파일 · 모듈)
- `direction` — 나가는 쪽(호출) · 들어오는 쪽(피호출) · 양쪽
- `depth` — 몇 홉까지
- `kinds` — call/ref/inherit/contains 중 볼 것만
- `granularity` — 모듈 · 네임스페이스 · 타입 · 메서드. 이 수준으로 **간선을 말아 올린다** (개별 호출 300개 → 「A→B, 가중치 300」 하나)
- `budget` — 노드 상한(기본 2,000) · 심볼당 차수 상한(기본 50)
- `truncated` — 예산에 걸려 잘린 자리를 **숨기지 말고 그대로 돌려준다** (「+312곳 더」)

확장은 Go 가 아니라 **SQLite 의 재귀 CTE 안에서** 일어난다. 상한에 닿으면 거기서 멈춘다 — 1만 노드를 읽어 와서 자르는 게 아니라 애초에 2천 개만 읽는다.

**허브 처리.** `ILogger` 같은 노드는 피호출이 수천이다. 차수 상한에 걸리면 개별 간선 대신 「부르는 곳 3,412 (모듈 8개)」 요약만 준다. 펼치기는 사용자가 명시적으로 눌렀을 때만.

**이게 메모리 설계의 전부다.** 상주 메모리가 그래프 크기가 아니라 **예산 크기**에 비례한다. 리포가 10배 커져도 뷰 하나의 비용은 그대로다.

### 1. 시간축 — 가장 차별화되는 부분

- 커밋(혹은 날짜) 단위로 그래프 **스냅샷**을 저장한다.
- 두 스냅샷의 **차이**를 보여준다: 새로 생긴 타입 / 사라진 타입 / 새로 생긴 의존 / 끊어진 의존.
- 질문 예시:
  - 「이번 주에 새로 생긴 의존이 뭐냐」
  - 「이 클래스를 부르는 곳이 언제부터 늘었냐」
  - 「경계를 넘는 참조가 새로 생겼냐」 ← 아키텍처 규칙 감시로 바로 이어진다

### 2. 전체 지도 → 파고들기

- 처음 화면은 **프로젝트/모듈 단위의 3D force 그래프.** 이 규모는 노드 수십~수백 개라
  전체를 한 장에 띄워도 읽힌다. graphify 가 잘하던 지점이고, 가져올 값이 있다고 판단했다.
- **클릭은 «더 불러오기» 가 아니라 «다시 묻기» 다.** 누른 노드가 §0 `subgraph` 의 새 `seed` 가
  되고 같은 원시연산을 다시 부른다. 화면에 있는 것을 늘리는 게 아니라 질문을 바꾸는 것이라
  **아무리 깊이 파고들어도 예산은 그대로다.** 네임스페이스 → 클래스 → 메서드로 좁혀 들어간다.
- 좁혀 온 경로는 breadcrumb 으로 남고 아무 단계로나 되돌아갈 수 있다.
  뒤로 갈 때도 새로 묻는 것이므로 상태를 쌓아 둘 필요가 없다.
- 선택한 심볼의 호출·피호출·상속을 옆에 소스와 함께 보여준다 (Sourcetrail 이 잘하던 것).

### 3. 에이전트 질의 (MCP)

- 코딩 에이전트가 파일을 뒤지기 전에 그래프에 먼저 묻게 한다.
- 도구 예: `find_symbol` · `callers_of` · `path_between` · `subgraph` · `changed_since(commit)`
- `changed_since` 가 특히 유용하다 — 에이전트에게 「지난 릴리스 이후 구조가 어떻게 변했나」를 한 번에 줄 수 있다.
- **에이전트에도 예산이 똑같이 걸린다.** 1만 노드를 토해내서 컨텍스트를 태우는 대신, 예산 안에서 답하고 「잘렸다」를 명시해 좁혀서 다시 묻게 한다. 응답은 사람이 읽는 모양이 아니라 토큰이 싼 모양으로 — 심볼 문자열 원문 대신 짧은 핸들.

---

## 만들지 않는 것 (범위 밖 — 이걸 지켜야 끝난다)

- ❌ 자체 언어 파서 — SCIP 색인기를 쓴다
- ❌ LLM · 임베딩 · 벡터 검색 — 그래프는 결정적이어야 한다
- ❌ 클라우드 · 계정 · 텔레메트리 — 전부 로컬, 네트워크 0
- ❌ IDE 플러그인 — 1.0 이후에 생각
- ❌ 코드 편집 기능 — 읽기 전용 도구다

---

## 아키텍처

```
소스 트리
   │
   ├─ scip-dotnet / scip-typescript  (외부 바이너리, 우리가 호출만)
   │        └─ index.scip
   │
   ├─ 적재기 ──► SQLite  (심볼 · 관계 · 위치 · 스냅샷)
   │                ▲
   │                └── git 메타 (커밋 해시 · 날짜 · 작성자)
   │
   ├─ 질의 코어  ── 부분 그래프 추출기(재귀 CTE) 하나가 전부
   │        ├─ 로컬 웹 UI  (정적 HTML + JS, 로컬 서버가 서빙)
   │        ├─ CLI
   │        └─ MCP 서버 (stdio)
```

### 기술 선택 (확정)

- **코어: C# (.NET 10)** — **성능으로 고른 게 아니다.** 실측상 코어가 하는 일은 파일을 읽어
  SQLite 에 넣고 JSON 을 뱉는 접착제고, 무거운 것은 전부 남의 코드에 있다
  (Roslyn 89초 : 우리 파싱 1.3초). 성능이 변별력을 잃으면 남는 기준은
  **«누가 몇 년간 고칠 수 있는가»** 뿐이고, 그건 팀의 모국어다. 시험대도 C# 이라
  dogfood 가 자연스럽다. 부품은 전부 성숙해 있다 — `Google.Protobuf` ·
  `Microsoft.Data.Sqlite` · `ModelContextProtocol`(공식 .NET MCP SDK).
- **저장: SQLite** — 파일 하나. Sourcetrail 도 SQLite 였고 그건 좋은 선택이었다.
- **UI: 정적 웹 + TypeScript** — 빌드 결과물을 실행 파일에 심는다.
  **런타임에 Node 가 필요하면 안 된다.** Qt 데스크톱 앱은 만들지 않는다 —
  Sourcetrail 의 배포 부담이 거기서 왔다.
- **그래프 그리기: 렌더러 두 개** — §0 의 `granularity` 가 레이아웃뿐 아니라 렌더러까지 가른다.

  | 단위 | 레이아웃 | 렌더러 |
  |---|---|---|
  | 모듈 · 네임스페이스 (전체 지도) | **3D force-directed** | WebGL (Three.js) |
  | 타입 · 메서드 (파고든 뒤) | 결정적 계층 배치 (`dagre`·`ELK.js`) | SVG |

  **왜 전체 지도만 3D 인가.** 3D 는 전체를 훑고 구조의 인상을 잡는 데 압도적이다.
  반면 **가려짐과 깊이 모호성 때문에 정밀한 읽기에는 불리하다** — 「이 클래스를 부르는
  12곳」 을 정확히 세는 일은 정밀 작업이고, 그건 기획서가 Sourcetrail 대비 우위로 잡은
  바로 그 지점이다. 그래서 파고든 뒤에는 2D 계층으로 갈아탄다.

  - **3D 도 재현 가능해야 한다.** 시드·반복 횟수에 더해 **초기 카메라까지 고정한다.**
    2D 보다 어긋나기 쉬우므로 더 엄격히 잡는다 — 「같은 코드는 매번 같은 그림」 은
    두 모드 모두에 걸린다.
  - 색은 어셈블리·네임스페이스로 칠한다. graphify 는 커뮤니티를 **추론해서** 색을 칠하지만
    우리는 **진짜 경계를 이미 갖고 있다.** 군집 알고리즘을 넣을 이유가 없다.

  ⚠️ **노드 2,000 상한의 근거가 바뀐다.** WebGL 은 10만 노드도 그리므로
  「SVG 가 못 그린다」 는 더 이상 이유가 아니다. 상한은 **그대로 두되** 근거를
  **「사람이 못 읽는다」 + 「SQLite 질의 비용」** 으로 바꾼다.
  예산은 렌더러의 한계가 아니라 **읽을 수 있음의 한계**다.

**탈락한 후보와 이유 (되풀이하지 않기 위해 적어 둔다):**

| | 왜 탈락했나 |
|---|---|
| **Go** | 배포(단일 정적 바이너리)와 SCIP 생태계(공식 구현이 Go)는 더 낫다. 팀 언어가 아니라는 이유 하나로 졌다. 이 판단이 틀렸다고 느껴지면 다시 열어도 되는 유일한 후보다 |
| **파이썬** | **속도는 문제가 아니다** — 손으로 짠 파서가 8.2MB 를 1.3초에 훑는다. 단일 실행 파일이 안 나오고, MCP 서버가 세션마다 뜨는데 인터프리터 기동이 붙는다 |
| **Rust** | 얻을 것이 없다. compute-bound 구간이 아예 없는 프로젝트다 |

**C# 으로 가면서 M0 에서 확인할 것 — 셋 다 실측으로 끝냈다:**

**1. 단일 실행 파일 — 된다. 단 NativeAOT 는 아직 아니다.**

자체 포함 단일 파일로 **52MB 실행 파일 하나**가 나오고, 옆에 아무것도 없이
CLI·웹 UI·MCP 가 전부 동작한다. 웹 페이지는 내장 리소스라 같이 들어간다.

- ⚠️ **`IncludeNativeLibrariesForSelfExtract` 를 켜야 한다.** 안 켜면 `e_sqlite3.dll` 이
  옆에 남는다 — 기획서가 걱정한 바로 그 지점이고, 실제로 그렇게 된다.
- **NativeAOT 는 실패한다.** 막는 것이 셋이다:
  리플렉션 기반 JSON 직렬화(익명 타입), 그리고 MCP SDK 의 `WithToolsFromAssembly` 어셈블리 스캔.
  통과시키려면 응답 타입을 전부 이름 있는 레코드로 바꾸고 `JsonSerializerContext` 를 붙인 뒤
  `WithTools<T>()` 로 갈아야 한다. **기계적이지만 작지 않은 작업이라 미뤄 뒀다.**
  질의·적재 경로의 직렬화는 이미 걷어냈다(정수 목록뿐이라 필요가 없었다).

**2. protobuf 스트리밍 — 된다.** `CodedInputStream` 으로 `Document` 를 하나씩 읽는다.
생성 코드의 `Index.Parser.ParseFrom()` 은 쓰지 않는다 — 통째로 힙에 올라온다.
전량 순회가 483ms · 최고 작업집합 42.6MB 로, 500MB 예산에 한참 못 미친다.

**3. 기동 시간 — 포장 방식과 무관하게 1~2초.**

| | 기동 |
|---|---|
| 프레임워크 의존 | 1.3 ~ 2.2초 |
| 단일 파일 · 압축 | 1.5 ~ 2.3초 |
| 단일 파일 · 무압축(104MB) | 0.8 ~ 3.7초 |

**편차가 방식 간 차이보다 크다** — 이 머신의 디스크·백신 영향으로 보인다.
MCP 서버는 세션마다 한 번 뜨므로 이 정도는 받아들일 만하다.
**다만 AOT 를 해야 할 진짜 이유가 여기 있다** — AOT 면 이 값이 수십 ms 가 된다.

---

## SCIP 실측 — 색인기가 실제로 주는 것

> `Acme.Shop` / `scip-dotnet` 0.2.14 / 2026-09-25.
> 문서 361 · occurrence 87,552 · SymbolInformation 11,481 · `index.scip` 7.42MB · 109초.

**추측으로 데이터 모델을 짜지 않기 위해 먼저 열어 봤다. 그 결과 아래 다섯 가지가 설계를 바꾼다.**

| | 준다 | | 안 준다 |
|---|---|---|---|
| `range` | ✅ `[줄, 시작칸, 끝칸]` | `kind` | ❌ 없음 |
| `symbol` 문자열 | ✅ 평균 52.3B | `display_name` | ❌ 없음 |
| `symbol_roles` | ✅ `Definition` 비트만 | `enclosing_symbol` | ❌ **0 / 11,481** |
| `relationships` | ✅ 1,462 (상속·구현) | `enclosing_range` | ❌ **0개** |

### 1. 스트리밍은 그냥 된다 — 전처리가 필요 없다

최상위가 `Index` 메시지 하나이고 `documents` 가 연속된 length-delimited 레코드다.
앞에서부터 태그 단위로 하나씩 읽으면 된다. **문서 최대 0.34MB · 중간값 10KB** 라
상주 메모리가 1MB 아래로 잡힌다.

`Document.text` 는 **0 바이트** — 소스 원문을 넣지 않는다. 7.42MB 가 전부 그래프 데이터다.

→ 「어려운 지점 §3 ①」 에 적어 둔 *"단일 메시지로 뱉으면 쪼개는 전처리를 둔다"* 는
**C# 에서는 필요 없다.** TS·Python 에서 다시 확인한다.

### 2. `kind` 는 심볼 문자열에서 결정적으로 복원된다

SCIP 가 `kind` 를 안 주지만 잃은 것은 아니다. 심볼 문자열의 접미가 곧 종류다.

| 예시 | 종류 |
|---|---|
| `App/App#` | 타입 |
| `App/App#_db.` | 필드·프로퍼티 |
| `App/App#IsCorruptLocalDb().` | 메서드 |
| `App/App#IsCorruptLocalDb().(ex)` | 매개변수 |
| `System/` | 네임스페이스 |
| `local 0` | 메서드 내부 지역 |

파싱이지 추정이 아니다. **포함 관계(`contains`)도 같은 문자열에서 나온다** — `App/App#_db.` 는
`App/App#` 안이다. 별도 간선을 저장할 필요 없이 심볼 문자열로 계산된다.

### 3. ⚠️ 호출 간선의 «누가» 가 없다 — 이게 진짜 급소다

occurrence 는 「이 위치에 심볼 B 가 나온다」만 말한다. **그게 어느 메서드 안인지는 안 알려준다.**
`enclosing_symbol` 과 `enclosing_range` 가 둘 다 비어 있기 때문이다.

즉 `callers_of` 의 **to 쪽은 공짜지만 from 쪽은 우리가 위치로 추론해야 한다.**

실측상 추론은 가능하다 — 정의가 소스 순서대로 나오므로 「이 참조를 감싸는 것은 가장 가까운
앞선 정의」 규칙이 성립한다. 다만 정확도가 층마다 다르다:

- **타입 단위 — 견고하다.** 한 파일의 참조는 그 파일이 정의하는 타입에 속한다. C# 관례상
  파일당 타입 하나가 보통이고, 아니어도 타입은 수가 적어 경계가 뚜렷하다.
- **메서드 단위 — 휴리스틱이다.** 중첩 타입 · 람다 · 로컬 함수 · 프로퍼티 접근자 ·
  필드 이니셜라이저에서 깨진다.

→ §0 의 `granularity` 와 정확히 맞물린다. 구조는 안 바꿔도 되지만
**메서드 단위 호출 그래프에는 「추정」 표시를 UI 에 달아야 한다.**
「추정 비율을 숨기지 말라」 는 원칙이 느슨한 언어만의 문제가 아니라 **C# 에서도 적용된다.**

### 4. occurrence 의 36.7% 는 그래프에 안 쓰인다

| | 개수 | 비율 |
|---|---|---|
| `local N` (메서드 내부 지역) | 22,668 | 25.9% |
| 네임스페이스 조각 | 9,418 | 10.8% |
| **간선 후보** | **55,466** | **63.4%** |

`using System.Diagnostics;` 한 줄이 `System/` · `Diagnostics/` 로 조각마다 occurrence 를 만든다.
적재기에서 버린다 — 안 버리면 그래프가 의미 없는 파일→네임스페이스 간선으로 덮인다.

### 5. `--allow-global-symbol-definitions` — 켜야 하고, 켜면 새 문제가 나온다

플래그를 끄고 켜서 **두 번 색인해 비교했다.**

| | 기본 | `--allow-global` |
|---|---|---|
| 파일 크기 | 7.42MB | 8.17MB (**+10%**) |
| 심볼 문자열 평균 | 52.1B | 57.5B (**+10%**) |
| documents · occurrences · SymbolInformation | 361 · 87,552 · 11,481 | **전부 동일** |
| 패키지가 빈 심볼 | 35,984 | **6,933** |

**그래프는 한 글자도 안 바뀌고 심볼 문자열만 완전해진다.** 값이 10% 큰 파일뿐이다.
남은 6,933개는 **전부 네임스페이스**라 어차피 버리는 것들이다(§4). 즉 의미 있는 심볼은
빠짐없이 패키지 식별자를 얻는다:

```
상점앱 1.0.0.0                        App/
Acme.Shop.Core 1.0.0.0    Core/
Microsoft.EntityFrameworkCore 10.0.11.0 EntityFrameworkCore/DbContext#
```

#### ⚠️ 그런데 버전이 심볼 ID 에 박힌다 — 이게 시간축을 부순다

`<Version>1.0.0</Version>` 을 `1.1.0` 으로 올리는 순간 **그 어셈블리의 모든 심볼 문자열이
바뀐다.** 구간 저장 모델에서는 「전부 죽고 전부 새로 태어났다」로 기록된다.
**릴리스 한 번에 그래프 전체가 갈린다.** 의존성 업그레이드(EF Core `10.0.11.0` → `10.0.12.0`)도
똑같다.

**결론: 저장 키에서 버전을 뗀다.**

- 심볼 ID = `scheme + manager + package + descriptor` — **버전 제외**
- 버전은 심볼의 **속성**으로 따로 둔다

부작용이 아니라 이득이다. 「이 의존성이 언제 버전이 올랐나」 가 별도 사실로 남아서
시간축이 답할 수 있는 질문이 하나 늘어난다.

#### 패키지 이름은 어셈블리 이름이지 프로젝트 이름이 아니다

App 프로젝트는 디렉터리가 `Acme.Shop.App\` 인데 패키지는 **`상점앱`** 이다
(`<AssemblyName>`). 증분 갱신의 「이 파일이 어느 프로젝트인가」 판단은 **폴더 이름이 아니라
`AssemblyName` 으로 이어야 한다.** 비ASCII 어셈블리 이름이 그대로 들어오므로
심볼 문자열에 ASCII 를 가정하지 말 것.

---

## 데이터 모델 (초안)

```
snapshot(id, ord, commit_sha, committed_at, indexed_at)   -- ord = first-parent 순회로 매긴 단조 증가 정수
symbol(id, scip_symbol, kind, name, container_id, file_id, line)
edge_life(from_id, to_id, kind, born_ord, died_ord)       -- 스냅샷마다 복제하지 않는다
symbol_life(symbol_id, born_ord, died_ord)
```

**간선을 스냅샷마다 복제하지 않는다.** 이게 이 프로젝트에서 제일 중요한 저장 결정이다.

초안대로 `edge(snapshot_id, …)` 로 두면 커밋 100개 × 간선 20만 = **2천만 행**이고, 그중 99%는 「지난 커밋과 똑같다」는 말의 반복이다. 대신 **구간으로 저장한다**: 간선이 태어난 `born_ord` 와 사라진 `died_ord` 만 적는다(살아 있으면 `died_ord IS NULL`). 죽었다 되살아나는 간선은 행을 하나 더 쓴다.

- 저장량이 「그래프 크기 × 커밋 수」에서 **「변경량의 합」** 으로 떨어진다. 커밋 하나가 실제로 건드리는 간선은 보통 수십~수백이다.
- 시간축 질의가 사실상 공짜가 된다:
  - 어느 시점의 그래프 → `born_ord <= T AND (died_ord IS NULL OR died_ord > T)`
  - 「지난주에 새로 생긴 의존」 → `born_ord > T` ← 스냅샷 둘을 떠서 비교하는 게 아니라 인덱스 한 번
  - 「이 클래스를 부르는 곳이 언제부터 늘었나」 → `to_id` 로 좁힌 뒤 `born_ord` 히스토그램
- **`ord` 가 정수인 이유가 이것이다.** 커밋 해시로는 부등호를 못 쓴다. 이력이 선형이 아니므로 first-parent 순회로 순번을 매기고, 그 순번이 시간축 질의의 축이 된다.

**문자열은 한 번만 저장한다.** SCIP 심볼 문자열은 100바이트를 넘는 게 흔하다. 이걸 간선마다 들고 있으면 안 된다. `symbol` 에서 한 번 인턴하고 그 뒤로는 전부 `int64` 두 개로만 오간다. 파일 경로·컨테이너도 마찬가지로 인턴한다. 간선 한 줄이 문자열 200바이트에서 정수 몇 개로 줄어드는 차이다.

⚠️ **심볼 ID 안정성이 이 프로젝트의 급소다.** 파일이 옮겨지거나 이름이 바뀌어도 같은 심볼로 이어져야 시간축이 의미를 갖는다. SCIP 심볼 문자열을 그대로 키로 쓰되, 이름 변경 추적은 별도 과제로 둔다.
**그리고 색인기를 `--allow-global-symbol-definitions` 로 돌려야 한다** — 안 그러면 심볼에 패키지가 안 붙어서 프로젝트를 따로 색인한 순간 ID 가 갈린다.
**단, 그 플래그가 붙여 주는 «버전» 은 키에서 떼어 내야 한다** — 안 떼면 릴리스 한 번에 전 심볼이 죽고 되살아난다. 둘 다 「SCIP 실측 §5」 에 실측과 함께 있다.

**`symbol.kind` 는 SCIP 가 주지 않는다.** 심볼 문자열 접미를 파싱해 채운다(실측 §2). 결정적이라 신뢰해도 된다.

**`edge.kind = call` 은 SCIP 에 없는 파생 간선이다.** occurrence 위치와 정의 위치로 우리가 계산한다(실측 §3). `contains` 는 심볼 문자열에서, `inherit` 은 `relationships` 에서 그대로 나온다 — 이 둘은 파생이 아니다.

---

## 어려운 지점 (미리 인정하고 시작)

### 1. 증분 갱신

SCIP 색인기도 기본은 전량이다. 우리가 할 일:

- git diff 로 바뀐 파일 집합을 구한다
- 그 파일이 속한 **프로젝트만** 다시 색인한다 (C# 은 어셈블리 단위가 자연스러운 경계)
- 결과를 기존 그래프에 갈아끼운다

~~목표: 전량 2분 이내~~ — **실측으로 폐기했다.** `scip-dotnet` 은 `.cs` 632개(프로젝트 4개)에
**109초**를 쓴다. 이 시간은 Roslyn 이 실제로 컴파일하는 시간이라 **우리가 줄일 수 있는 구간이 아니다.**
같은 트리의 큰 C# 저장소(`.cs` 11,385개)로 선형 외삽하면 30분대이고, 그건 Sourcetrail 의 22분과 같은 영역이다.

**따라서 전량 색인 속도는 목표로 삼지 않는다. 증분이 이 프로젝트의 전부다.**

- 전량 — 목표 없음. 걸리는 만큼 걸린다. 한 번만 하면 되는 일이다.
- ~~증분 10초 이내~~ — **이것도 실측으로 폐기했다.**

**증분 실측 (`Acme.Shop`, 바뀐 파일 2개 → 프로젝트 1개):**

| | 시간 |
|---|---|
| 전량(솔루션) | 89초 |
| 프로젝트 하나 · restore 포함 | 55.2초 |
| 프로젝트 하나 · **restore 건너뜀** | **16.6초** |
| 그래프에 갈아끼우기 | **0.4초** |

**바닥은 색인기가 정한다.** 16.6초는 Roslyn 이 그 프로젝트와 의존을 컴파일하는 시간이고,
문서 5개짜리 제일 작은 프로젝트가 그렇다. 우리 쪽 작업(대조·적재)은 0.4초로 무시할 만하다.

- **`--skip-dotnet-restore` 가 제일 큰 레버였다** — 55초를 16초로 만들었다.
  작업 트리는 이미 복원돼 있는 것이 보통이므로 기본으로 건너뛰고,
  패키지를 새로 추가했을 때만 `--restore` 로 켠다.
- 우리가 더 줄일 수 있는 것은 **다시 색인할 프로젝트를 최소로 고르는 것** 하나뿐이다.
  여러 프로젝트가 걸리면 병렬 색인이 남아 있지만, 흔한 경우(프로젝트 하나)에는 도움이 안 된다.
- **10초 아래로 가려면 색인기가 바뀌어야 한다** — 상주 프로세스로 컴파일을 재사용하는 방식이다.
  그건 우리 코드가 아니라 `scip-dotnet` 의 설계다. 목표로 걸지 않는다.

### 2. 그래프 시각화

1만 노드를 그대로 그리면 털뭉치가 된다 — **다만 그건 «세밀한 단위» 의 문제지 레이아웃의 문제가 아니다.**
모듈·네임스페이스로 말아 올리면 이 리포가 노드 수십~수백 개고, 그 규모에서는 force-directed 가
구조를 제일 잘 보여준다. 전체 지도는 그렇게 그린다.

즉 털뭉치를 막는 것은 레이아웃 선택이 아니라 **예산과 `granularity`** 다. 화면에 그리는 것은
언제나 §0 의 부분 그래프 한 덩어리이고, 파고들어 세밀해지는 순간 결정적 계층 배치로 갈아탄다.

> 이 절은 원래 「force 레이아웃에 의존하지 말 것」 이었다. 과했다.
> 거친 단위에서는 force 가 맞고, 그게 graphify 의 전체 지도가 잘 읽히는 이유다.
> 전체 지도는 **3D** force 로 간다 — 기술 선택 절 참고.

### 3. 메모리 — 큰 리포에서 제일 먼저 죽는 곳

C# 1.8만 심볼은 아무렇게나 짜도 돈다. 문제는 그다음이다 — TS 모노레포나 큰 공개 리포에서 심볼이 수십만이 되면 순진한 구현은 그냥 죽는다. 나중에 고치기 제일 비싼 종류라 처음부터 지킨다.

**① SCIP 를 통째로 언마샬하지 않는다.** `index.scip` 는 protobuf 하나에 리포 전체가 들어 있다. `proto.Unmarshal` 한 방이면 파일 크기의 몇 배가 힙에 올라온다. 길이 구분(length-delimited) 스트림으로 `Document` 를 **하나씩** 읽어 배치로 SQLite 에 밀어 넣고 즉시 버린다. 상주 메모리는 「가장 큰 문서 하나 + 배치 버퍼」로 고정된다.
색인기가 단일 메시지로만 뱉는 경우에는 그 앞단에 쪼개는 전처리를 우리가 한 번 둔다. **색인기 출력 형태를 M0 에서 반드시 확인할 것** — 여기서 스트리밍이 불가능하면 메모리 계획 전체가 흔들린다.

**② 그래프 객체를 Go 힙에 만들지 않는다.** 인접 리스트도 노드 맵도 없다. **SQLite 가 곧 그래프다.** 힙에 사는 것은 방금 뜬 부분 그래프(≤ 예산)뿐이고, 응답을 보내면 사라진다.

**③ 증분 갱신도 프로젝트 단위로 스트리밍한다.** 바뀐 어셈블리만 다시 색인하고 그 프로젝트의 문서만 갈아끼운다. 색인은 병렬로 돌리되 **적재는 단일 writer** 로 직렬화한다 — SQLite 쓰기 경합도 피하고 메모리 최고점도 낮춘다.

**④ UI 에는 부분 그래프만 보낸다.** 한 뷰의 JSON 상한 1MB. 목록은 가상 스크롤.
전체 지도는 3D force(WebGL), 파고든 뒤는 결정적 계층 배치(SVG) — 둘 다 시드를 고정해
「같은 코드는 매번 같은 그림」 을 지킨다. **렌더러가 WebGL 이라고 해서 더 많이 보내지 않는다** —
예산은 렌더러가 아니라 사람과 질의 비용에 걸려 있다.

**⑤ SQLite 를 제대로 켠다.** WAL · `mmap_size` 넉넉히 · 관계 테이블은 `WITHOUT ROWID` · 질의마다 커버링 인덱스 · 적재는 큰 트랜잭션. mmap 을 쓰면 읽기 쪽 메모리 부담이 OS 페이지 캐시로 넘어가고, 압박이 오면 커널이 알아서 회수한다. 우리가 캐시를 직접 만들지 않는다.

**예산 (넘으면 버그로 취급)**

| | 상한 |
|---|---|
| 적재 중 상주 메모리 | 500MB — **리포 크기와 무관하게** |
| 질의·서빙 중 상주 메모리 | 200MB |
| 부분 그래프 한 번 | 노드 2,000 · JSON 1MB · 300ms |

**부분 그래프 실측** (`Acme.Shop`, 전부 예산 안):

| 뷰 | 노드 · 간선 | JSON | 시간 |
|---|---|---|---|
| 전체 지도 · 모듈 | 38 · 66 | 7.2KB | 67ms |
| 전체 지도 · 네임스페이스 | 105 · 328 | 26.6KB | 90ms |
| 파고들기 · 타입 2홉 | 384 · 2,933 | 201.7KB | 182ms |

전체 지도는 **씨앗이 이미 그 층 전부**라 넓혀도 새 노드가 나오지 않는다.
그걸 알아채고 홉 질의를 건너뛰는 것만으로 네임스페이스 지도가 465ms 에서 90ms 가 됐다.
| DB 파일 | 커밋 100개 이력을 포함해 소스 트리 크기 이하 |

---

## 마일스톤

| | 목표 | 완료 기준 |
|---|---|---|
| **M0** ✅ | SCIP 를 스트리밍으로 읽어 SQLite 에 넣는다 | CLI 로 「이 클래스를 부르는 곳」 목록이 정확히 나오고, 적재 중 메모리가 리포 크기를 따라가지 않는다 |
| **M1** ✅ | 부분 그래프 + 웹 UI — 전체 지도 → 파고들기 | 실제 리포에서 모르는 코드를 5분 안에 이해할 수 있다. 심볼 20만짜리 리포에서도 뷰 하나가 300ms 안에 뜬다 |
| **M2** ✅ | 시간축 | 「지난 100커밋 동안 새로 생긴 의존」 이 표로 나오고, DB 가 커밋 수에 비례해 부풀지 않는다 |
| **M3** ✅ | MCP + 증분 | 에이전트가 질의하고, 바뀐 프로젝트만 다시 색인해 갈아끼운다. 시간은 색인기가 정한다(실측 17초) |

**네 개 다 섰다.** 남은 것은 완성도지 구조가 아니다 —
NativeAOT(직렬화 정리 필요) · TypeScript 시험대 · Python 시험대 ·
그리고 「20만 심볼에서 300ms」 는 아직 그만한 리포로 재 보지 못했다.
지금까지의 최대는 `Acme.Shop` 의 8,220 심볼이다.

M0 은 며칠, M1~M2 가 이 프로젝트의 본체다.

---

## 검증 방법 (dogfood)

### 시험대 세 종류

언어를 늘리는 일이 «색인기 하나 갈아끼우기» 로 끝난다는 설계 전제를, 말이 아니라 **실제로 세 언어를 돌려서** 증명한다.

| | 대상 | 규모 | 색인기 | 이 시험대로 확인하는 것 |
|---|---|---|---|---|
| **1. C#** | `Acme.Shop` | 프로젝트 4개 · `.cs` 632개 | `scip-dotnet` | **주 개발 대상.** 정확도의 기준선. 한 바퀴가 109초라 반복이 현실적이다 |
| **2. Node / TS** | `AcmeShopWeb` | `.ts/.tsx` 235개 (Next.js) | `scip-typescript` | 컴파일 단위가 어셈블리가 아닌 언어에서 «프로젝트 경계» 를 어떻게 잡을지. 증분 갱신의 진짜 시험대 |
| **3. Python** | 공개 리포 하나 (중간 규모) | — | `scip-python` | 타입이 느슨한 언어에서 정확도가 얼마나 떨어지는지. 「정밀」 주장의 한계선을 정직하게 긋는다 |

**1·2 를 같은 도메인의 짝으로 고른 것은 의도한 것이다.** 데스크톱 C# 앱과
Next.js 웹이 같은 업무를 다루므로, 두 그래프를 보면서 «언어가 달라도 같은 개념이
같은 모양으로 나오는가» 를 눈으로 비교할 수 있다.

### ✅ 확장 시험대 — 큰 C# 저장소로 재 봤다

`.cs` 11,385개 · csproj 183개. **한 바퀴가 3분 28초였다** — 30분대로 예상했던 것이 틀렸다.

| | Acme.Shop | 큰 C# 저장소 |
|---|---|---|
| `index.scip` | 8.2MB | **34.5MB** |
| documents · occurrences | 361 · 87,552 | 1,776 · **358,782** |
| 심볼(서로 다른) | 8,220 | **43,932** |
| 간선 | 31,384 | **185,266** |
| 스캔 시간 · 메모리 | 483ms · 41.6MB | 798ms · **41.6MB** |
| 적재 시간 · 메모리 | 2.0초 · 52MB | 8.2초 · **104MB** |
| DB | 4.3MB | 35.2MB |

**스트리밍이 증명됐다.** 색인이 4배 커졌는데 스캔 메모리가 41.6MB 로 **똑같다.**
적재 메모리도 104MB 로 500MB 예산 안이다.

**질의 시간은 300ms 예산을 살짝 넘는다:**

| 뷰 | 노드 · 간선 | 시간 |
|---|---|---|
| 모듈 | 115 · 524 | 327ms |
| 네임스페이스 | 406 · 3,646 | 337ms |
| 타입(전체 층) | 2,000 · 8,000 (잘림) | 870ms |

모듈·네임스페이스는 예산 언저리이고 쓸 만하다. **타입 층 전체 지도는 이 규모에서
쓰라고 있는 뷰가 아니다** — 예산에 걸려 씨앗 1,606개와 간선 14,045개가 잘린다.
그게 맞는 동작이고, 그래서 파고들기가 있는 것이다.

⚠️ **여기서 진짜 버그가 나왔다.** 「씨앗이 이미 그 층 전부면 넓힐 것이 없다」 는 지름길이
**예산 검사까지 건너뛰고 있었다.** 작은 리포에서는 씨앗이 예산보다 적어 드러나지 않았고,
이 규모에서 노드 3,606개 · JSON 2.3MB 가 예산을 무시하고 나갔다.
씨앗과 간선 양쪽에 예산을 걸어 고쳤다 — **큰 리포로 재 보지 않았으면 못 찾았을 종류다.**

### ✅ 2번 시험대 — 돌았다. 그리고 **코드를 한 줄도 고치지 않았다.**

> ⚠️ 원래 여기에는 위험이 적혀 있었다 — `scip-typescript` 0.4.0 은 `typescript ^5.6.2` 에
> 의존하는데 대상은 `typescript 7.0.2` · `next 16` 이다. **실제로 돌려 보니 문제없었다.**
> 자기가 들고 있는 TypeScript 로 파싱하며, 41초에 6.18MB 색인을 냈다.

**이 프로젝트의 핵심 설계 전제가 여기서 증명됐다.** C# 용으로 쓴 적재기·파서·질의가
TypeScript 색인을 그대로 먹었다. 분기도, 언어별 처리도 없다.

| | C# (Acme.Shop) | TypeScript (ShopWeb) |
|---|---|---|
| 색인 | 89초 · 8.2MB | 41초 · 6.2MB |
| documents · occurrences | 361 · 87,552 | 248 · 62,209 |
| 서로 다른 심볼 | 8,220 | 6,385 |
| 적재 | 2.0초 | 5.2초 |
| 「모듈」이 뜻하는 것 | 어셈블리 | **npm 패키지** |

「모듈」 층이 **어셈블리에서 npm 패키지로 저절로 바뀐다** — SCIP 의 package 칸이
언어마다 알아서 채워지기 때문이다. 전체 지도에
웹 패키지에서 그것이 쓰는 npm 패키지들로 가는 선이, 쓰는 만큼의 굵기로 나온다.

**다만 두 언어의 «결» 이 다르고, 그게 숫자에 드러난다:**

- **귀속 못 한 참조가 1.4% → 14.8%** 로 는다(672 → 5,879). TS 는 파일 맨 위에
  `import` 와 모듈 수준 코드가 정의보다 앞서 오는 일이 훨씬 흔하다.
  「가장 가까운 앞선 정의」 규칙이 C# 보다 덜 먹힌다는 뜻이다.
- **상속 간선이 1,462 → 12** 로 준다. TypeScript 는 구조적 타입이라
  `implements` 관계를 색인기가 거의 내보내지 않는다.
  **이 언어에서는 상속 그래프를 기대하면 안 된다.**

같은 도메인을 고른 것이 값을 했다 — `YearMonth` 가 양쪽에 다 있어서
`treering callers` 로 C# 쪽과 TS 쪽을 나란히 볼 수 있다.

세 색인기 모두 현재 유지되고 있다. **라이선스도 확인했다 — 셋 다 문제없다.**

| 색인기 | 버전 · 최근 배포 | 라이선스 |
|---|---|---|
| `scip-dotnet` | 0.2.14 · 2026-05 이후 | Apache-2.0 |
| `scip-typescript` | 0.4.0 · 2026-06-30 | Apache-2.0 |
| `scip-python` | 0.6.6 | **MIT** |

기획서에 「`scip-python` 은 별도 조건」 이라고 적어 뒀던 것은 틀렸다. MIT 다.
셋 다 허용적이라 기업 사용을 막지 않는다 — 우리가 Apache-2.0 으로 가는 데 걸림돌이 없다.

**다만 우리는 색인기를 재배포하지 않는다.** 사용자가 각자 설치하는 외부 바이너리다
(`dotnet tool install` · `npm install -g`). 라이선스가 문제되는 것은 언젠가 묶어서
배포할 때이고, 그때를 위해 미리 확인해 둔 것이다.

> ⚠️ **개발 순서와 검증 순서는 다르다.** 코드는 C# 하나로 시작하되(M0~M2), **데이터 모델을 확정하기 전에 세 언어의 SCIP 출력을 한 번씩 열어 볼 것.** C# 에만 맞춘 모델을 짜면 나중에 전부 뒤집는다. C# 은 어셈블리라는 깔끔한 경계가 있지만 Python·JS 에는 그게 없다 — 증분 갱신 설계가 여기서 갈린다.

### 측정할 것

- 전량 색인 시간 — **목표가 아니라 기록용.** (실측: Acme.Shop 632 파일 = 109초 / `index.scip` 7.42MB)
- 증분 갱신 시간 — 언어별로 따로 잰다
- 심볼/관계 수가 컴파일러 기준과 맞는지
- 언어별 «추정(inferred)» 비율 — 이 수치를 숨기지 말고 UI 에 그대로 노출할 것
- **적재 중 메모리 최고점** — 리포 크기를 3배로 키웠을 때 같이 3배가 되면 스트리밍이 어딘가 깨진 것이다
- **부분 그래프 지연** — 평범한 뷰 / 허브 심볼 / 최대 예산, 세 경우를 따로. 꼬리(p99)를 본다
- **커밋당 DB 증가량** — 구간 저장이 실제로 먹히는지는 이 수치 하나로 드러난다
- 네트워크 송신 0 확인

---

## 라이선스

Apache-2.0 권장. SCIP 자체가 Apache-2.0 이고, 기업 사용을 막지 않는 게 채택에 유리하다.

---

## 이름 후보

| 후보 | 뜻 | 평 |
|---|---|---|
| **Treering** (나이테) | 나무의 나이테 = 시간이 쌓인 구조 | 시간축이라는 핵심을 한 단어로 담는다. AST(tree) 말장난도 된다. GitHub 동명 리포 86개로 적은 편 |
| **Codestrata** | 코드 지층 | 거의 안 쓰인다(리포 8개). 시간=퇴적층 은유. 조금 길다 |
| **Chronoscope** | 시간을 보는 기구 | 뜻은 가장 정확. 길고 발음이 무겁다 |
| **Strata** | 지층 | 짧고 좋지만 이미 6천 개 넘게 쓰인다 |

**추천: Treering.** 한국어로 「나이테」라 부르기도 좋고, 이 도구가 남들과 다른 지점(시간)을 이름이 바로 설명한다.

---

## 다음 한 걸음

~~`scip-dotnet` 을 돌려서 SCIP 가 무엇을 주는지 본다~~ — **끝났다.** 결과는 「SCIP 실측」 절에 있다.

**이제 M0 적재기를 쓴다.** 순서는 이렇다.

1. ~~`--allow-global-symbol-definitions` 로 다시 색인한다~~ — **끝났다.** 결과는 실측 §5.
   켜고 쓰되 **버전을 뗀 키**를 쓴다.
2. **스트리밍 적재기**: `Document` 를 하나씩 읽어 SQLite 에 배치로 넣는다.
   심볼 문자열 인턴 + 접미 파싱으로 `kind` 채우기까지가 한 덩어리다.
3. **잡음 필터**: `local N` 과 네임스페이스 조각을 버린다(36.7%).
4. **`contains` · `inherit`** 을 먼저 만든다. 파생이 아니라 그대로 나오는 것들이다.
5. **`call` 파생**: 「가장 가까운 앞선 정의」 로 from 쪽을 붙이고,
   **타입 단위와 메서드 단위의 신뢰도를 따로 기록한다.**
6. CLI 로 「이 클래스를 부르는 곳」 이 정확히 나오면 M0 완료다.

**그다음 검증 두 가지 (데이터 모델을 확정하기 전에):**

- `scip-typescript` 가 `AcmeShopWeb`(TS 7)에서 **돌기는 하는지**
- TS 출력에도 `enclosing_range` 가 비어 있는지 — 비어 있지 않다면
  C# 쪽 휴리스틱이 언어별 예외였다는 뜻이고, 그건 좋은 소식이다
